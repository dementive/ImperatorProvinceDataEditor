# Building the project

To build setup_data_manager.py:

1. Run "python -m auto_py_to_exe"

2. Open the "Settings" section and click "Import Config From JSON File".

3. Select the build_config.json file

4. Click the button to build